# Copy to .env (gitignored) or inject via the platform. No real secret here.
PGHOST=db
PGPORT=5432
PGDATABASE=twin
PGUSER=twin
# PGPASSWORD is delivered by Vault, never written here.
PGPASSWORD=
APP_VERSION=station2-dev
# Must equal the highest applied migration or the service refuses readiness.
EXPECTED_SCHEMA_VERSION=3
GRACEFUL_DRAIN_SECONDS=3
