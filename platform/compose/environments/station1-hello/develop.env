# Environment-specific config for station1-hello, develop environment.
# Injected via `docker compose --env-file` by platform/compose/deploy.sh — the
# pilot's image is never rebuilt per environment (see Plan.md §4 "兩環境最小隔離":
# production-like promotes the exact same image digest validated in develop).
APP_VERSION=station1-develop
